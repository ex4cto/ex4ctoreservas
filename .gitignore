# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Environment / secrets
.env
*.local

# OS
.DS_Store
Thumbs.db

# Runtime
logs/
*.log

# Local databases (dev/staging populated by scripts/seed.py + scripts/importar_ventas.py)
*.db

# One-off invoice render preview (regenerable por el servicio de factura)
ejemplo_factura.html

# Media / user data (reference only, not tracked)
*.xlsx
*.xls
*.jpg
*.jpeg
*.png
*.pdf
# Exception: compressed logo committed to repo for Railway deployment
!assets/logo.png

# Tool config (local only)
.atl/
.claude/

# Business context docs (local reference, not versioned)
"situacion problema"
situacion problema
GARAY_CONTEXT.md
